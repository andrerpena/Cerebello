@model CerebelloWebRole.Areas.App.Models.MedicineLeafletViewModel
@{
    Layout = null;
    var editPanel = this.Html.CreateEditPanel(null);
    editPanel.AddField(m => m.Description);
    editPanel.AddField(m => m.Url);
}
@using (Html.BeginCollectionItem("Leaflets"))
{
    @Html.HiddenFor(model => model.Id)
    @editPanel.GetHtml()
}
