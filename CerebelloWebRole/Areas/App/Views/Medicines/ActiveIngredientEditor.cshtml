@model MedicineActiveIngredientViewModel
@{
    Layout = null;
}
@using (Html.BeginCollectionItemInline("ActiveIngredients"))
{
    @Html.HiddenFor(model => model.Id)
    @Html.EditorFor(model => model.Name)
}
