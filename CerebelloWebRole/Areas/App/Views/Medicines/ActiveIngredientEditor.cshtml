@model CerebelloWebRole.Areas.App.Models.MedicineActiveIngredientViewModel
@{
    Layout = null;
}
@using (Html.BeginCollectionItemInline("ActiveIngredients"))
{
    @Html.AutocompleteFor(model => model.ActiveIngredientId, model => model.ActiveIngredientName, @Url.Action("AutocompleteActiveIngredients"), @Url.Action("CreateModal", "ActiveIngredients"), "Novo Princípio Ativo", 500, 100)
}
